---
- name: Configure Nginx on nginx group
  hosts: nginx
  become: true
  gather_facts: true

  roles:
    - nginx

- name: Configure Apache on apache group
  hosts: apache
  become: true
  gather_facts: true

  roles:
    - apache
